export type InputThemeColorType = {
  textColor: string;
  backgroundColor: string;
  borderColor: string;
  labelColor: string;
  hintColor: string;
  errorColor: string;
  placeholderColor: string;
};
